services:
  app:
    build: .
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    environment:
      APP_ENV: local
      APP_DEBUG: true

  test:
    build: .
    command: vendor/bin/phpunit
    volumes:
      - .:/var/www/html
